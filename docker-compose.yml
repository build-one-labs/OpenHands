services:
  openhands:
    build:
      context: ./
      dockerfile: ./containers/app/Dockerfile
    image: openhands:latest
    container_name: openhands-app-${DATE:-}
    hostname: openhands-app
    environment:
      - BETTER_AUTH_URL=${BETTER_AUTH_URL:-https://auth-next.test.build.one}
      - CODESPACE_NAME=${CODESPACE_NAME:-}
      - GITHUB_CODESPACES_PORT_FORWARDING_DOMAIN=${GITHUB_CODESPACES_PORT_FORWARDING_DOMAIN:-}
      - AGENT_SERVER_IMAGE_REPOSITORY=${AGENT_SERVER_IMAGE_REPOSITORY:-ghcr.io/openhands/agent-server}
      - AGENT_SERVER_IMAGE_TAG=${AGENT_SERVER_IMAGE_TAG:-31536c8-python}
      #- SANDBOX_USER_ID=${SANDBOX_USER_ID:-1234} # enable this only if you want a specific non-root sandbox user but you will have to manually adjust permissions of ~/.openhands for this user
      - WORKSPACE_MOUNT_PATH=${WORKSPACE_BASE:-$PWD/workspace}
      - OH_SANDBOX__RESOURCE_PREFIX=${OH_SANDBOX__RESOURCE_PREFIX:-openhands}
      - OH_SANDBOX__NETWORK=openhands-net
      - OH_SANDBOX__APP_HOSTNAME=openhands-app
      - 'OH_SANDBOX_CONTAINER_LABELS={"com.docker.compose.project": "openhands"}'
      - OH_SANDBOX__PRIVILEGED=true
      - OH_SANDBOX__DIND_REGISTRY_MIRROR_URL=http://openhands-registry-cache:5000
      - OH_SANDBOX__DIND_REGISTRY_MIRRORS=docker.cloudsmith.io=http://openhands-cloudsmith-cache:5000
      - SANDBOX_VOLUMES=/var/run/docker.sock:/var/run/docker.sock
      - BETTER_AUTH_SECRET=${BETTER_AUTH_SECRET:-}
      - LLM_MODEL=${LLM_MODEL:-anthropic/claude-opus-4-5-20251101}
      - LLM_BASE_URL=${LLM_BASE_URL:-}
      # --- Env vars forwarded into sandbox containers (OH_SANDBOX_FWD__ prefix is stripped) ---
      - OH_SANDBOX_FWD__AUTH_URL=${BETTER_AUTH_URL:-https://auth-next.test.build.one}
      - OH_SANDBOX_FWD__BETTER_AUTH_SECRET=${BETTER_AUTH_SECRET:-}
      - OH_SANDBOX_FWD__B1_AUTOMATION_HUB_EMAIL=${B1_AUTOMATION_HUB_EMAIL:-}
      - OH_SANDBOX_FWD__B1_AUTOMATION_HUB_PASSWORD=${B1_AUTOMATION_HUB_PASSWORD:-}
      - OH_SANDBOX_FWD__OPENAI_API_KEY=${OPENAI_API_KEY:-}
    ports:
      - "3000:3000"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ~/.openhands:/.openhands
      - ${WORKSPACE_BASE:-$PWD/workspace}:/opt/workspace_base
    networks:
      - openhands-net
    pull_policy: build
    stdin_open: true
    tty: true
    depends_on:
      - registry-cache
      - cloudsmith-cache

  registry-cache:
    image: registry:2
    container_name: openhands-registry-cache
    environment:
      - REGISTRY_PROXY_REMOTEURL=https://registry-1.docker.io
    volumes:
      - openhands-registry-cache:/var/lib/registry
    networks:
      - openhands-net
    restart: unless-stopped

  cloudsmith-cache:
    image: registry:2
    container_name: openhands-cloudsmith-cache
    environment:
      - REGISTRY_PROXY_REMOTEURL=https://docker.cloudsmith.io
      - REGISTRY_PROXY_USERNAME=${BUILDONE_USER:-}
      - REGISTRY_PROXY_PASSWORD=${BUILDONE_TOKEN:-}
    volumes:
      - openhands-cloudsmith-cache:/var/lib/registry
    networks:
      - openhands-net
    restart: unless-stopped

volumes:
  openhands-registry-cache:
  openhands-cloudsmith-cache:

networks:
  openhands-net:
    name: openhands-net
    driver: bridge
